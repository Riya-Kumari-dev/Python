# tab
str = "Riya\tGupta"
print(str)
# next line
str2 = "Riya Gupta\nA very passionate student"
print(str2)

# concatenation => adding two strings
s = "Aditi"
finalString = str2+"\n"+s
print(finalString)

# length of string
print(len(str))

# swap case
# capital => small , small -> capital
print(s.swapcase())

# indexing
print(str[2]) # will print y
# str[2] = a  # python doesnot support index modification

# endswith("word") => return true if string ends with "word"
print(str.endswith("ta"))

# str.captilaze => capitalizes the 1st character of the string
s = "itya"
s.capitalize()
print(s) # it will be itya as while captilazing it is creating a new string
s = s.capitalize()
print(s)

# str.replace(old, new) => replaces all occurence of old with new one
str = str.replace("Gupta","Kumari") 
print(str)

# str.find(word) => returns the 1st index of the occurer
# string.rfind(word) => returns the last index of the occurer
# we can use index() (similar to find()) also at the place of find but if the substr is not present it will show Value Error
print(str.find("at")) # if not present prints -1
print(str.find("ya"))
# str.find(substr, start idx, end idx+1) 
# => search in start idx and end idx
t = "Eita Riya karela kanjus" 
print(t.find("Riya",3,9)) # 5


# str.count(word) => counts the occurence of word
print(str.count("a"))
print(str.count("a",2,6))

# positive integer multiplication will print the string that integer times 
# negative integer and zero multiplication will print empty string
print(s*3) # will print s 3 times 
print(s*-1) # empty string
print(s*0) # empty string

# in (Membership operator)
# check whether it is a substring or not of the given string

s = "Riya is a bright student."
print("is" in s)
print("b" in s)
print("r" in s)